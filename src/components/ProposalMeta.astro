---
interface Props {
  client: string;
  date: Date;
  status: 'draft' | 'sent' | 'accepted' | 'archived';
  tags?: string[];
}

const { client, date, status, tags } = Astro.props;

const statusLabels = {
  draft: 'Rascunho',
  sent: 'Enviado',
  accepted: 'Aceito',
  archived: 'Arquivado',
};

const formattedDate = new Date(date).toLocaleDateString('pt-BR', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});
---

<!-- Metadata: appears after title -->
<div class="mb-12 -mt-4 print:mb-8 print:mt-0">
  <div class="flex flex-wrap items-center gap-x-3 text-sm text-gray-500">
    <span class="font-medium text-gray-900">{client}</span>
    <span class="text-gray-300">·</span>
    <span>{formattedDate}</span>
    <span class="text-gray-300">·</span>
    <span>{statusLabels[status]}</span>
  </div>
</div>
